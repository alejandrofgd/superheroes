import { SuperheroImagePipe } from './superhero-image.pipe';

describe('SuperheroImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SuperheroImagePipe();
    expect(pipe).toBeTruthy();
  });
});
